# F2.7-B03-R8 — AUTHORITATIVE MULTI-ROLE ONE-SHOT UAT V2 REPORT

**Authority**: KLOPP / HQ DIRECTIVE  
**Mission**: F2.7-B03-R8 — AUTHORITATIVE MULTI-ROLE ONE-SHOT UAT V2  
**Canonical Base Commit**: `0f9924468339a6ea028670b1f04e9285e21b4a24`  
**Branch**: `uat/F2.7-B03-authoritative-one-shot-v2`  
**Mode**: ACCEPTANCE + EVIDENCE ONLY (Strict Implementation Prohibition: Zero source/test/DB changes)  
**Execution Date**: 2026-08-28  

---

## 1. Executive Summary & Verdict

In strict compliance with **KLOPP / HQ Directive Section 8 (Database Reset / Load)** and **Section 18 & 26 (One-Shot Stop Rule)**:

1. Environment preflight verified with `PLAYWRIGHT_RUNTIME_OK`.
2. Database reset executed exactly once (`pnpm supabase db reset`).
3. Scratch fixture `b03_fixture_v2.sql` was authored with `programme.status = 'Active'` as explicitly instructed in Section 5.
4. When applying `b03_fixture_v2.sql` to PostgreSQL, PostgreSQL returned:
   ```text
   ERROR: invalid input value for enum programme_lifecycle_status: "Active"
   LINE 6: ...AT-PROG-A', 'Projek UAT Bangunan Pentadbiran JKR', 'Active',
   ```
5. Schema inspection of PostgreSQL confirmed that `enum programme_lifecycle_status` contains only:
   `('Draft', 'Approved', 'Archived', 'UnderReview', 'Superseded')`.
   In the repository codebase architecture (`src/repositories/ProgrammeRepository.ts`), the application layer maps domain `status=Active` (from `GET /api/programme?status=Active`) to PostgreSQL DB column `status='Approved'` via:
   ```typescript
   const databaseStatus = params?.status === 'Active' ? 'Approved' : params?.status;
   ```
6. Because the database enum does not accept `'Active'`, the fixture transaction aborted.
7. Per Section 8 (*"If ANY mismatch: STOP BEFORE BROWSER. No fixture correction/reload after first load."*), browser execution was **NEVER STARTED** (invocation count = 0).

**Final Classification**: **`INVALID — TEST HARNESS FAILURE`**

---

## 2. Environment Preflight Verification

- Execution Command Test: `$env:NODE_PATH=(Resolve-Path ".\node_modules").Path; node -e "const { chromium } = require('@playwright/test'); if (!chromium) process.exit(1); console.log('PLAYWRIGHT_RUNTIME_OK')"`
- Output: `PLAYWRIGHT_RUNTIME_OK` (exit code 0)

---

## 3. Fixture Load Error & Pre-Browser Stop Log

```text
ERROR: invalid input value for enum programme_lifecycle_status: "Active"
LINE 6: ...AT-PROG-A', 'Projek UAT Bangunan Pentadbiran JKR', 'Active',
                                                              ^
QUERY: INSERT INTO public.programme (
        programme_id, programme_code, programme_name, status,
        created_by, created_at, updated_at
    ) VALUES (
        'a0000000-0000-4000-8000-000000000001',
        'UAT-PROG-A', 'Projek UAT Bangunan Pentadbiran JKR', 'Active',
        '99999999-9999-4999-8999-999999999994',
        now(), now()
    ) ON CONFLICT (programme_id) DO UPDATE
        SET status = 'Active',
            programme_name = 'Projek UAT Bangunan Pentadbiran JKR',
            updated_at = now()
CONTEXT: PL/pgSQL function inline_code_block line 8 at SQL statement
```

---

## 4. Pre-Run Database Validation Counts

- **`programme` count for Programme A**: `0`
- **`programme_revision` count**: `0`
- **`programme_membership` count**: `0`
- **`task` count**: `0`
- **`activity` count**: `0`
- **`site_diary` count**: `0`
- **`approval` count**: `0`
- **Browser Runner Invocations**: `0`
- **Browser Journeys**: `0`
- **Post-Runtime DB Cleanup / DELETE**: `NONE`

---

## 5. Governance Declarations

1. **Database Reset Count**: `1`
2. **Fixture Load Attempt Count**: `1`
3. **Runner Invocations**: `0`
4. **Browser Journeys**: `0`
5. **Direct API Substitute Used**: `NONE`
6. **`service_role` Key Used**: `NO`
7. **Production Environment Touched**: `NO`
8. **Source Code Changed**: `NO`
9. **Test Code Changed**: `NO`
10. **Database Migration / Seed / GRANT / RLS / RPC Changed**: `NO`
11. **Old Failure Branch `uat/F2.7-B03-authoritative-one-shot` Touched**: `NO`
12. **PR #54 Touched**: `NO`
13. **PR #51 Touched**: `NO`
