# F2.7-B03R6 — Site Diary Approval Request + Terminal Observation UI Closure Report

## 1. Locked Root Causes

1. **Gap A (P1 Approval Request UI Action Missing)**:
   After a successful Site Diary creation, `DailyEntryFeedback` only rendered Print preview, Aktiviti Terbuka, and Laporan Baharu. There was no user-facing UI action to request approval. As a result, previous test runners bypassed the UI and made direct API calls to `POST /api/approval`.
2. **Gap B (P2 Terminal Observation Surface Missing)**:
   In `ApprovalReview`, upon a successful PATCH approval decision, the component immediately invoked `onSuccess()`, navigating away without giving the authorised reviewer a terminal success surface to visibly inspect the Approved status, site_diary_id, approved_by, and approval comment.
3. **Response Token Capture in CREATE mode**:
   `submitDailyEntry` in CREATE mode did not populate `lastModifiedAt` from the successful `POST /api/site-diary` response payload, which is required as `expected_site_diary_last_modified_at` for approval request concurrency safety.

---

## 2. Architecture Implemented

### Part A — P1 Approval Request
- In `DailyEntryFeedback`:
  - When non-edit Site Diary creation succeeds, the component receives authoritative identity context (`programmeId`, `revisionId`, `activityId`, `siteDiaryId`, `lastModifiedAt`).
  - Renders explicit `"Mohon Kelulusan"` action button.
  - Submits `POST /api/approval` with exact required payload:
    - `programme_id`
    - `revision_id`
    - `activity_id`
    - `site_diary_id`
    - `expected_site_diary_last_modified_at`
  - Zero client actor identity fields sent (`requested_by`, `approved_by`, `actorId`, `userId` remain strictly server-derived).
  - On HTTP 201: Captures `approval_id` and transitions to explicit `"Menunggu Kelulusan"` Pending state indicator, disabling/removing the request button to prevent duplicate submission.
  - On failure: Surfaced safe canonical error and allows manual retry.

### Part B — CREATE Response Token
- In `DailyEntryForm.tsx` (`submitDailyEntry`):
  - In CREATE mode, canonical response token is captured strictly in priority order:
    1. `data.lastModifiedAt`
    2. `data.updated_at`
    3. `data.submitted_at`
  - No timestamp fabrication. Token is stored in state and passed to approval context as `expected_site_diary_last_modified_at`.

### Part C — P2 Terminal Observation
- In `ApprovalReview.tsx`:
  - On successful `PATCH /api/approval/[id]` with `status === 'Approved'`, the returned Approval data is parsed and stored in `terminalApproval` state.
  - Does NOT automatically navigate away.
  - Renders a terminal success surface on the same review page displaying:
    - Status: Approved / Diluluskan
    - Exact `site_diary_id`
    - `approved_by`
    - `approval_comment` (if present)
    - `approval_date`
  - Provides an explicit `"Kembali ke Kelulusan"` button which invokes `onSuccess()`.
  - Non-Approved decisions (Returned / Rejected) preserve existing immediate callback behavior.

---

## 3. Changed Files Matrix

### Production Files (3 files)
1. `src/app/site-diary/DailyEntryFeedback.tsx`
2. `src/app/site-diary/DailyEntryForm.tsx`
3. `src/app/site-diary/ApprovalReview.tsx`

### Test Files (2 files)
4. `tests/integration/ui/dailyEntryFeedback.test.ts`
5. `tests/integration/ui/approvalReview.test.ts`

**Total source/test count**: Exactly 5 files (within the 5 files maximum ceiling).

---

## 4. Test & Verification Results

- **Focused Tests**:
  - `tests/integration/ui/dailyEntryFeedback.test.ts`: 24/24 passed (including Tests A-G).
  - `tests/integration/ui/approvalReview.test.ts`: 21/21 passed (including Tests H-J).
  - Total focused tests: 45 passed.
- **Preflight Verification (`CI-HARDEN-001` / `pnpm run verify`)**:
  - Frozen lockfile consistency: PASS
  - TypeScript validation (`tsc --noEmit`): PASS
  - API TypeScript validation (`tsc --noEmit -p tsconfig.api.json`): PASS
  - ESLint: PASS
  - Automated test suite (`vitest run`): PASS (126 test files, 934 tests passed)
  - Production build (`next build`): PASS (29 static/dynamic routes)
- **Whitespace & Diff Check (`git diff --check`)**: PASS (0 whitespace errors)

---

## 5. Focused Runtime Proof — ONE Journey Evidence

### Fixture Used
- Programme: `a0000000-0000-4000-8000-000000000001` (`Projek UAT Bangunan Pentadbiran JKR`)
- Revision: `b0000000-0000-4000-8000-000000000002` (Rev 2, Approved)
- Activity: `d0000000-0000-4000-8000-000000000001` (`Kerja Konkrit Struktur Utama`)
- Date: `2026-08-28`
- P1: `submitter@jkr.gov.my` (`99999999-9999-9999-9999-999999999991`)
- P2: `reviewer@jkr.gov.my` (`99999999-9999-9999-9999-999999999992`)

### Step 1: P1 UI Site Diary Creation & Approval Request
1. P1 logged in via UI and navigated to Programme A (`a0000000-0000-4000-8000-000000000001`).
2. Selected `Aktiviti Terbuka` tab and clicked `Sambung Laporan` for activity `d0000000-0000-4000-8000-000000000001`.
3. Filled form details (Location: `Aras 2, Zon Timur, Blok Utama Grid 4-8`, Notes: `Kemajuan kerja konkrit struktur utama berjalan lancar.`) and submitted via UI button `Hantar & Simpan Buku Harian Tapak`.
4. Captured `POST /api/site-diary` returned **`HTTP 201 Created`**.
   - Created `site_diary_id`: `5199d499-0b73-4918-bb0a-22f4884a61d9`
5. Observed `"Mohon Kelulusan"` action button rendered in `DailyEntryFeedback`.
6. Clicked `"Mohon Kelulusan"` in UI.
7. Captured browser-generated `POST /api/approval` returned **`HTTP 201 Created`**.
   - Created `approval_id`: `069db2b1-5e9e-49fe-afc3-44d208c371b4`
   - Initial Status: `Pending`
   - Captured Payload:
     ```json
     {
       "programme_id": "a0000000-0000-4000-8000-000000000001",
       "revision_id": "b0000000-0000-4000-8000-000000000002",
       "activity_id": "d0000000-0000-4000-8000-000000000001",
       "site_diary_id": "5199d499-0b73-4918-bb0a-22f4884a61d9",
       "expected_site_diary_last_modified_at": "2026-08-28T13:59:17.856571+00:00"
     }
     ```
   - Client Actor Identity: None sent (`requested_by` server-derived as P1).
8. Observed `"Menunggu Kelulusan"` badge visible in UI, request button disabled/removed to prevent duplicate submission.

### Step 2: P2 UI Review, Approval & Terminal Observation
1. P2 logged in via UI and navigated to Programme A.
2. Selected `Kelulusan` tab, viewed pending Approval Queue item for `Kerja Konkrit Struktur Utama`.
3. Clicked `Semak (Review)` to load `ApprovalReview` for Site Diary `5199d499-0b73-4918-bb0a-22f4884a61d9`.
4. Entered review comment: `Kerja konkrit disemak dan diluluskan.`
5. Clicked `Luluskan (Approve)` via UI.
6. Captured `PATCH /api/approval/069db2b1-5e9e-49fe-afc3-44d208c371b4` returned **`HTTP 200 OK`**.
7. Remained on terminal observation surface on the same review page.
8. Visibly observed:
   - Status: `Approved (Diluluskan)`
   - Exact `site_diary_id`: `5199d499-0b73-4918-bb0a-22f4884a61d9`
   - `approved_by`: `99999999-9999-9999-9999-999999999992` (P2)
   - `approval_comment`: `Kerja konkrit disemak dan diluluskan.`
9. Explicitly clicked `"Kembali ke Kelulusan"`, successfully returning to the Approval Queue view.

---

## 6. Read-Only Database Control Verification

Direct read-only PostgreSQL query on local Supabase verified:
- **`public.site_diary`**: Exactly 1 row for `(activity_id, activity_date)`.
  - `site_diary_id`: `5199d499-0b73-4918-bb0a-22f4884a61d9`
  - `activity_id`: `d0000000-0000-4000-8000-000000000001`
  - `programme_id`: `a0000000-0000-4000-8000-000000000001`
  - `revision_id`: `b0000000-0000-4000-8000-000000000002`
  - `activity_date`: `2026-08-28`
  - `status`: `In Progress`
  - `submitted_by`: `99999999-9999-9999-9999-999999999991` (P1)
  - `submitted_at`: `2026-08-28 13:59:17.856571+00`
- **`public.approval`**: Exactly 1 row.
  - `approval_id`: `069db2b1-5e9e-49fe-afc3-44d208c371b4`
  - `site_diary_id`: `5199d499-0b73-4918-bb0a-22f4884a61d9` (exact match)
  - `approval_status`: `Approved`
  - `requested_by`: `99999999-9999-9999-9999-999999999991` (P1)
  - `approved_by`: `99999999-9999-9999-9999-999999999992` (P2)
  - `approval_comment`: `Kerja konkrit disemak dan diluluskan.`
  - `approval_date`: `2026-08-28 14:01:47.541287+00`
- **Duplicate Approval Count**: `0` (Total approvals for activity = 1).

---

## 7. Governance & Scope Affirmations

1. `/api/programme-revision` touched: **NO**
2. DB migration changed: **NO**
3. Seed changed: **NO**
4. GRANT / RLS / RPC changed: **NO**
5. `service_role` used: **NO**
6. Failed Acceptance PR #54 touched/merged/updated: **NO**
7. Old PR #51 touched: **NO**
