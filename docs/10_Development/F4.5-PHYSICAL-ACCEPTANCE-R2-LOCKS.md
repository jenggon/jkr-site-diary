# F4.5 PHYSICAL ACCEPTANCE ROUND 2 LOCKS

**Status:** Product Owner accepted / LOCKED for the next F4.5 remediation cycle.

**Baseline candidate:** `18ffdb42815a71c05fe90abafd0c5516d7c05cf8`

**Doctrine:** `LOCK -> ISOLATE -> PROVE -> FREEZE SHA -> PROMOTE EXACT SHA -> REPROVE -> PHYSICAL ACCEPT -> SEAL`

This round supersedes only the presentation/copy decisions explicitly named below. Existing F4.5 domain, database, auth/security, Programme/Revision, REKOD, print, approval, NGAMSOI mark and official weather authority remain protected.

## PA2-01 — Executor field semantics

The CATAT field currently labelled `SKOP` is too vague for an operational user.

LOCK:
- user-facing label becomes `PELAKSANA`;
- immediate F4.5 values are `Kontraktor Utama` and `NSC`;
- do not redesign persistence/domain semantics in F4.5;
- deterministic organisation/package ownership is deferred to a Project Stakeholder Registry / Project Membership model;
- stakeholder role is project-specific and must not be inferred from a global user profile.

## PA2-02 — Four-fact mini dashboard

The operational mini dashboard contains exactly four user-facing facts:

1. `PROGRAM KERJA`
2. `TINGGAL`
3. `SEMASA`
4. `RAMALAN CUACA`

LOCK:
- remove `HARI KE` from the user-facing dashboard;
- remove redundant duplicated `PROGRAM KERJA` labelling;
- `TINGGAL` combines remaining number with `HARI` and current completion target, e.g. `207 HARI · SIAP 31/03/27`;
- Wide and Half: one four-column row;
- Phone: two-by-two responsive grid using the same four facts;
- no clipping, ellipsis or horizontal overflow.

## PA2-03 — Forecast visual parity

`RAMALAN CUACA` belongs inside the same tactical frame/system as the other three dashboard facts.

LOCK:
- no visually detached sidecar/strip treatment;
- same border/frame grammar as the other facts;
- forecast remains operational only and never mutates official CATAT/Page-1 weather;
- remove green accent/line from forecast because green is reserved for completed/success semantics;
- dry/rain/loading/unavailable states must not change dashboard geometry.

## PA2-04 — SUMBER sub-header grammar

`SUMBER` must use the same sub-header grammar as `HARIAN`, `TAPAK`, `CUACA`, `PEKERJA`, `CATATAN`:

- same orange dash motif;
- same typography hierarchy;
- same baseline/alignment;
- no one-off heading treatment.

Spine semantic alignment remains governed by F45-SPINE-002.

## PA2-05 — Official weather user-facing copy

For same-day/manual official CATAT weather:

- default `ELOK` does not display `Manual`;
- default `ELOK` does not display `Manual · Tiada hujan`;
- absence of rain is already expressed by `ELOK` and does not need duplicate explanatory copy;
- when HUJAN is selected, rain interval controls/details remain available;
- provider provenance remains data/audit authority, not mandatory user-facing noise.

For historical/provider-assisted weather:

- before supervisor confirmation, user-facing state may say `Cadangan cuaca`;
- after SAH/UBAH, user-facing state is `Disahkan`;
- provider technical metadata remains secondary and must not compete with the operational decision.

F45-WX-001, F45-WX-002 and F45-WX-003 remain fully protected.

## PA2-06 — Actual Start helper language

Keep the field label `Mula sebenar` where Actual Start is required.

Replace technical helper language such as `Bukan tarikh MSP.` with user-context copy:

`Tarikh sebenar kerja mula di tapak.`

Do not expose implementation/planning terminology merely to explain the field.

Underlying Activity lifecycle and Actual Start semantics remain unchanged.

## PA2-07 — Same-day start-and-complete copy

Do not show the green `Mula + Siap` status note.

The selected MULA/SIAP controls already communicate the same-day lifecycle state. Green remains reserved for meaningful completed/success state, not redundant explanatory copy.

Underlying `MULA_DAN_SIAP` domain semantics remain unchanged.

## PA2-08 — Completion Seal

Save success remains a focused completion checkpoint in the current viewport, but its presentation becomes a restrained `Completion Seal` rather than an ordinary dialog/toast.

LOCK:
- success hero remains `Disimpan`;
- secondary brand copy remains `Kena boh! Ngamsoi.`;
- `Tunjuk Rekod` and `Tambah Aktiviti` remain required actions;
- add an explicit `×` close/dismiss button;
- no timer-driven disappearance;
- no automatic scroll-to-top or other Save-triggered page movement;
- dismissal closes the overlay without undoing the saved state;
- SIMPAN remains the completed final Spine checkpoint after dismissal;
- completion symbol uses the same visual DNA as a completed Spine node: circular green success node with check, scaled appropriately for the seal;
- restrained ceremonial resolve is allowed (short, non-blocking transition), but no confetti or decorative noise;
- backdrop/topmost/sharp geometry remain;
- keyboard focus remains bounded and accessible;
- canonical `site_diary_id/siteDiaryId`, persistence, auth and domain semantics remain unchanged.

## PA2-09 — Deferred stakeholder architecture direction

Future deterministic executor/stakeholder identity will use project-scoped membership rather than global profile semantics:

`PERSON -> PROJECT MEMBERSHIP -> ORGANISATION -> PROJECT ROLE`

This is an accepted architecture direction for future work only. F4.5 must not introduce database, migration, RLS/RBAC or auth changes to implement it.

## Protected boundaries

No change without separate Product Owner authority:

- NGAMSOI mark geometry;
- REKOD #7 redesign, daily aggregation or approval redesign;
- print semantics / official daily output aggregation;
- approval semantics;
- domain/Engine ownership;
- database, migrations, RLS/RBAC or authentication/security;
- Programme/Revision authority and no-cross-revision rules;
- official weather provider/domain authority;
- Activity lifecycle semantics including `MULA_DAN_SIAP`;
- canonical Site Diary identity.

## Physical acceptance status

Round 1 candidate `18ffdb42815a71c05fe90abafd0c5516d7c05cf8` remains a valid known-green technical baseline but is **NOT SEALED** because Product Owner physical acceptance identified the above Round 2 changes.

The next candidate must return through the full Proven Promotion Protocol and receive fresh Product Owner physical acceptance before F4.5 can be SEALED.
