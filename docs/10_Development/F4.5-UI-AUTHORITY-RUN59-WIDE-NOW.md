# F4.5 UI Authority — Run #59 Wide SEMASA Remediation

Status: IMPLEMENTATION RECORD — pending fresh CI and Codex re-audit. AG remains HOLD.

## LOCKSET COMPLIANCE ACK

BRANCH=feature/F4.5-ui-authority-cutover
BASE_HEAD=497b64458c2022f12924899f585592640ffaba76
LOCKSET_VERSION=2026.09.04.2
LOCKSET_HASH=e7127cfe9dfdd5b1dcbb8a563242aa6da3c0c332bd42e16dc6998d143e38a34e
ROLE=BOUNDED_REMEDIATION
AFFECTED_IDS=F45-HDR-002,F45-CSS-001,F45-CI-001,F45-CI-002
NO_CHANGE_BOUNDARIES=NGAMSOI mark geometry; REKOD #7; print; approval; domain/Engine ownership; database/migrations/RLS/RBAC/authentication/security; Programme/Revision authority

The active lockset remains unchanged. No locked requirement is relaxed, superseded, or bypassed.

## Run #59 evidence

GitHub Actions run #59 / `33891559693` evaluated exact HEAD `497b64458c2022f12924899f585592640ffaba76`.

Passed before browser acceptance:

- lockset verification PASS — version `2026.09.04.2`, semantic hash `e7127cfe9dfdd5b1dcbb8a563242aa6da3c0c332bd42e16dc6998d143e38a34e`, 24 LOCKED requirements;
- unified verification PASS;
- 151 test files / 1204 tests PASS;
- production build PASS;
- Chromium installation PASS.

Real-browser result: 2 PASS / 1 FAIL. Half-window and phone passed. The phone path continued to exercise the real Save flow and persistent post-save geometry evidence.

The remaining failure was wide desktop only, and only the `now` / SEMASA core fact:

- text: `SEMASA JUMAAT 04/09/26 · 23:50`;
- `outsideBounds=true`;
- `ellipsized=false`;
- `clipped=false`;
- `emptyFact=false`;
- `clientWidth=251`, `scrollWidth=251`;
- `clientHeight=38`, `scrollHeight=38`.

The prior `programme` clipping detected in run #58 is resolved by the 38rem wide pulse allocation.

Failure artifact:

- artifact ID `9944124735`;
- name `f45-browser-failure-33891559693`;
- SHA256 `fdb8fec5697bf9eb422bf02f58935d7505d3df56094d84ec21fa4e6b9d39df26`.

Screenshot review confirmed that the wide header has spare horizontal capacity while the SEMASA fact reserves right-side space for the secondary forecast badge. The remaining issue is therefore allocation, not missing content, ellipsis, or a test-harness defect.

## Bounded correction

Only `src/app/ngamsoi-f45-authority.css` is changed for runtime behavior.

Inside the existing `@media (min-width: 1200px)` authority rule:

`min-width: 38rem !important;`

becomes:

`min-width: 46rem !important;`

No markup, typography, label, alignment, forecast behavior, medium/phone rule, test assertion, threshold, retry, timeout, fixture, protected business semantics, or additional stylesheet authority is changed.

Before branch update, the candidate CSS was created off-branch and compared against BASE_HEAD. GitHub reported exactly one modified file with 1 addition and 1 deletion, proving the intended one-line substitution only.

## Gate

A fresh production CI run on the resulting exact HEAD must pass unified verification, Chromium installation, and all three real-browser viewport cases with retries remaining zero. CI green does not mean SEALED. Codex must then independently re-audit the exact final HEAD. AG remains HOLD until Codex passes, and Product Owner physical acceptance remains mandatory under F45-REL-001.
