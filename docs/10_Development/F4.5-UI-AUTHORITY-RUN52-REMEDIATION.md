# F4.5 UI Authority — Run #52 Bounded Remediation Record

Status: IMPLEMENTATION RECORD — automated verification and independent agent sign-off are mandatory before SEALED.

## LOCKSET COMPLIANCE ACK

BRANCH=feature/F4.5-ui-authority-cutover
BASE_HEAD=1d69740e63b81266422128c5d52deffe6bcafb5a
LOCKSET_VERSION=2026.09.04.2
LOCKSET_HASH=e7127cfe9dfdd5b1dcbb8a563242aa6da3c0c332bd42e16dc6998d143e38a34e
ROLE=REMEDIATION
AFFECTED_IDS=F45-WF-001,F45-HDR-001,F45-HDR-002,F45-GEO-001,F45-CSS-001,F45-CI-001,F45-CI-002
PROTECTED_IDS=F45-SEM-001,F45-ID-001,F45-STATUS-001,F45-STATUS-002,F45-WX-001,F45-WX-002,F45-WX-003,F45-WF-002,F45-TIME-001,F45-NAV-001,F45-RESP-001,F45-SPINE-001,F45-VO-001,F45-SAVE-001,F45-BRAND-001,F45-SCOPE-001,F45-REL-001
NO_CHANGE_BOUNDARIES=NGAMSOI mark geometry; REKOD #7; print; approval; domain/Engine ownership; database/migrations/RLS/RBAC/authentication/security; Programme/Revision authority

The active lockset is unchanged by this remediation. No Product Owner requirement is changed or weakened.

## Trigger evidence

GitHub Actions F4.5 CATAT Corrections Gate:

- Run: `33882843880` / run #52
- Head: `1d69740e63b81266422128c5d52deffe6bcafb5a`
- Browser artifact: `f45-browser-failure-33882843880`
- Artifact digest: `sha256:32ce61e7fb2030e6296c923a6faeb2a40847bef8de085ccb17fc9a29cd7d4d11`
- Unified verification passed; the real-browser F4.5 UI authority gate failed.

Run #52 exposed three bounded findings:

1. Wide desktop hardhat assertion used the global `.ng-workforce__overall-icon` locator even though both SmartWorkforceEntry and its internal WorkforceEntry own instances in the DOM.
2. The generic header helper required usable forecast to be visible at half-window width even though the final authority intentionally hides forecast at 768–1199 px while preserving the four core operational facts.
3. Phone computed-style evidence found two operational INPUT elements at `border-radius: 6px`. F45-GEO-001 requires sharp geometry and therefore the browser assertion must remain strict.

## LOCKED REQUIREMENT IMPACT MATRIX

| Requirement | Treatment | Files |
| --- | --- | --- |
| F45-WF-001 | Preserve accepted hardhat glyph; scope browser evidence to the SmartWorkforceEntry total instead of an ambiguous global locator. | `tests/e2e/f45-ui-authority.e2e.spec.ts` |
| F45-HDR-001 / F45-HDR-002 | Keep the four core facts mandatory. Encode the accepted medium-width rule that forecast is secondary and hidden at 768–1199 px. | `tests/e2e/f45-ui-authority.e2e.spec.ts` |
| F45-GEO-001 | Keep zero-radius computed-style acceptance strict. Repair production CSS, not the threshold. | `src/app/ngamsoi-f45-authority.css` |
| F45-CSS-001 | Make the existing final authority directly own operational radius rather than depending on older seal/generic shell selectors. No new stylesheet is introduced. | `src/app/ngamsoi-f45-authority.css` |
| F45-CI-001 / F45-CI-002 | Preserve `next start`, real clicks, fixture allowlist, zero retries, no force/skip/timeout inflation/evidence weakening. | Existing CI/harness contract remains unchanged except bounded assertions above. |

## Exact implementation

### Browser harness

- `expectCoreHeader()` now detects the current Playwright viewport.
- Wide and phone: usable `dry/rain` forecast remains required visible; `loading/unavailable` remains hidden.
- Half-window 768–1199 px: forecast is required hidden while programme/revision, remaining days, day number and current date-time remain visible.
- Hardhat evidence is scoped to:
  `[data-testid="smart-workforce-entry"] .ng-workforce-smart__total .ng-workforce__overall-icon`
- The scoped glyph must resolve to exactly one visible element.

### Final production geometry authority

`src/app/ngamsoi-f45-authority.css` now explicitly owns zero radius for F4.5 operational:

- panels;
- form inputs, selects, textareas and buttons;
- segmented/weather controls and result/alert shells;
- desktop/mobile navigation buttons;
- operational project-pulse cells and forecast;
- post-save toast/actions.

The existing circular Spine nodes remain explicitly circular. Profile identity remains outside this new operational selector set and retains its accepted circular exception.

## Forbidden remediation methods — confirmed not used

- no `force: true`;
- no skipped/deleted locked assertions;
- no geometry/overflow threshold relaxation;
- no retries;
- no arbitrary timeout increase;
- no dev-overlay hiding;
- no preview/auth bypass;
- no production test route;
- no provider override;
- no domain/API/database/auth/REKOD/print/approval change;
- no additional visual authority stylesheet.

## Required verification before acceptance

The implementation is not complete until all of the following are recorded against the final remediation HEAD:

1. `pnpm run lockset:verify`
   - expected lockset version: `2026.09.04.2`
   - expected semantic hash: `e7127cfe9dfdd5b1dcbb8a563242aa6da3c0c332bd42e16dc6998d143e38a34e`
   - expected locked requirements: `24`
2. `pnpm run verify`
3. `pnpm exec playwright test tests/e2e/f45-ui-authority.e2e.spec.ts --project=chromium --retries=0` against the already-built `next start` runtime in CI.
4. Codex independent remediation review with the mandatory `LOCKSET COMPLIANCE ACK`.
5. Antigravity independent audit with the mandatory `LOCKSET COMPLIANCE ACK`.
6. Product Owner physical acceptance at wide desktop, half-window and phone.

Codex or Antigravity must not report PASS if branch, HEAD, lockset version/hash, changed-file set, protected boundaries, or verification results cannot be independently established.

`CI GREEN` is not `SEALED`. `SEALED` remains blocked until physical acceptance is green.
