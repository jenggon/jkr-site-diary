# F4.5 UI Authority — Run #58 Wide Header Readability

Status: BOUNDED PRODUCT REMEDIATION — pending fresh CI and Codex re-audit. AG remains HOLD.

## LOCKSET COMPLIANCE ACK

BRANCH=feature/F4.5-ui-authority-cutover
BASE_HEAD=50aa8052491d2c967e6d1033a29de136cf92a0d7
LOCKSET_VERSION=2026.09.04.2
LOCKSET_HASH=e7127cfe9dfdd5b1dcbb8a563242aa6da3c0c332bd42e16dc6998d143e38a34e
ROLE=REMEDIATION
AFFECTED_IDS=F45-HDR-002,F45-CSS-001,F45-CI-001
NO_CHANGE_BOUNDARIES=NGAMSOI mark geometry; REKOD #7; print; approval; domain/Engine ownership; database/migrations/RLS/RBAC/authentication/security; Programme/Revision authority

## Run #58 evidence

GitHub Actions run #58 / `33890377620` evaluated HEAD `50aa8052491d2c967e6d1033a29de136cf92a0d7`.

- Lockset verification: PASS — version `2026.09.04.2`, expected semantic hash, 24 locked requirements.
- Typecheck / API typecheck / lint / unit+integration suite / production build: PASS.
- Test suite: 151 files / 1,204 tests PASS.
- Chromium installation: PASS.
- Real-browser gate: 2 PASS / 1 FAIL.

The failure was the new F45-HDR-002 readability evidence at wide desktop only. Medium and phone cases passed, including the real post-save flow and sharp toast evidence.

Wide runtime evidence reported:

- `programme`: client width 97 px, scroll width 104 px; child text outside cell bounds and actual horizontal clipping.
- `now`: client/scroll width 215 px; no scroll clipping, but visible text child bounds escaped the fact-cell boundary.
- no ellipsis was used.

Artifact: `f45-browser-failure-33890377620`, artifact ID `9943645082`, SHA256 `eba920f71d010b1dd48c3b2a2610cf67d3f01d56892c44670a7b04d437369890`.

The screenshot shows substantial unused horizontal space between the project identity and pulse, so this is a genuine wide-layout allocation defect rather than a need to shrink typography or weaken the browser gate.

## Bounded correction

Only the existing consolidated `src/app/ngamsoi-f45-authority.css` is changed. At viewport widths >=1200 px, the operational project pulse receives a 38rem minimum width. This reallocates existing wide-header horizontal space to the four locked facts and secondary forecast while preserving:

- existing labels and typography;
- the four core facts;
- secondary forecast behavior;
- medium 768–1199 rules;
- phone rules;
- sharp geometry;
- one F4.5 runtime authority.

No browser assertion or readability threshold is relaxed.

## Files changed in this follow-up

- `src/app/ngamsoi-f45-authority.css`
- `docs/10_Development/F4.5-UI-AUTHORITY-RUN58-WIDE-HEADER.md`

AG remains HOLD until fresh CI is green and Codex re-audits the exact resulting HEAD.
