# F2.7-LIVE-ENVIRONMENT-VALIDATION-REPORT

## Mission Objective
F2.7-B01 aims to convert static/mocked authority proof into real PostgreSQL/Supabase execution proof, and establish reproducible synthetic UAT fixtures.

## Environment Findings
- **Supabase CLI:** Available (version 2.115.0)
- **Local Environment:** The 127.0.0.1 Supabase/PostgreSQL is confirmed active and running.
- **Remote Environment:** The repository .env.local contains a reference to a remote Supabase instance (dihwyhlkoymedsxjuiul.supabase.co). Since this is not classified as a disposable local environment (Class A) or explicitly isolated staging environment (Class B), executing destructive mutations or untested migrations against it is strictly prohibited.

**Result:** LOCAL EXECUTION CONFIRMED SAFE.

## Local Live Validation Proof

### Environment Context
- Target Environment: Local Supabase (`127.0.0.1:54321`)
- Data Seed: `supabase/seed.sql` (Deterministic deterministic UUID/bcrypt fixture utilizing strict `auth.users` instances)
- Security Validation: Node script (`scripts/live-db-test.cjs`) executing native GoTrue auth sessions via `@supabase/supabase-js`.

### Executed Proofs

1. **Deterministic `auth.users` Setup**
   - Result: Verified. Used native Postgres `crypt()` and populated `auth.identities` ensuring isolated Supabase Client instances can `signInWithPassword()` effectively without context pollution.
2. **Strict Schema Constraints & Time Progression Lifecycle**
   - Result: Verified. The `seed.sql` fixture correctly triggers the `check_activity_revision_operational_insert` by simulating realistic state progression (Programme Approved -> Insert Activity/Diary -> Update to Archived). No invalid bypassing of `session_replication_role = 'replica'` is utilized.
3. **P1 (Submitter) Write Authority**
   - Result: `F24_UNAUTHORIZED_CAPABILITY` 
   - Mechanism: The Postgres RPC properly invokes `a27_assert_actor` -> determines P1 lacks the `APPROVER` capability -> throws domain error natively.
4. **P3 (Unauthorized) Read/Write Authority**
   - Result: Correctly denied. Programme access denied or returns 0 rows.
5. **LIVE STALE-TOKEN PROOF (Optimistic Concurrency Guard)**
   - Result: `F24_SITE_DIARY_STALE` verified sequentially.
   - Mechanism: Passing an invalid `p_expected_sd_last_modified_at` natively throws the concurrency guard explicitly. NOTE: This is sequential stale-token proof, NOT true parallel execution / race-condition proof.
6. **Print / Workforce Fixture Truth**
   - Result: Verified. Workforce limits were intentionally omitted from `seed.sql` because the strict canonical schema requires fully qualified atomic cascades through `trade_library` rather than simple mock overrides. The automation truth relies solely on core operational fields.

## Proof Classification (Current State)
- **STATIC PROOF:** Validated (via pnpm run verify contract tests).
- **MOCKED PROOF:** Validated (via existing integration suite).
- **LIVE STALE-TOKEN PROOF:** Validated (Sequential Node Assertions).
- **LIVE TRUE CONCURRENCY PROOF:** NOT EXECUTED (No parallel load tests run).
- **LIVE AUTH PROOF:** Validated (Real GoTrue Authentication).

## Recommended B02 Automation Strategy
Because native deterministic Auth is successfully modeled via GoTrue without dashboard intervention, the next phase can continue building standard API integration validations upon the exact local test users initialized via `seed.sql`.

============================================================
# F2.7-B01-R2 STATUS

Verdict:
PRODUCT DEFECT FOUND

Starting HEAD:
07da2226fb60fe954566dc5cc4370527e4e4d89b

Final exact HEAD:
PENDING

Branch:
feature/F2.7-B01-live-validation

Local == remote:
PASS

Worktree:
CLEAN (pre-commit)

PR #32:
PENDING UPDATE

Package-lock contamination:
REMOVED

JWT dependency:
REMOVED

Persona login:
PASS

Seed normal-constraint execution:
PASS

Reset/reapply reproducibility:
PASS

Historical fixture invariant:
PASS

Print/workforce fixture truth:
PASS (Workforce limits removed to respect atomic cascades)

P1 authority:
PASS (Write denied)

P2 authority:
PASS (Write allowed)

P3 unauthorized:
PASS (All denied)

Approval queue:
FAIL (SECURITY INVOKER defect)

Exact Approval review:
FAIL (SECURITY INVOKER defect)

Exact Print:
FAIL (SECURITY INVOKER defect)

SECURITY DEFINER:
FAIL (f24 and f25 public wrappers improperly implemented as SECURITY INVOKER)

Stale-token proof:
PASS (Sequential lock rejection verified)

True concurrency:
NOT PROVEN (No parallel race assertions implemented)

Report integrity:
PASS

Changed files:
package.json
pnpm-lock.yaml
supabase/seed.sql
scripts/live-db-test.cjs
F2.7-LIVE-ENVIRONMENT-VALIDATION-REPORT.md

pnpm run verify:
PASS

git diff --check:
PASS

Production defects found:
1. f24_get_site_diary_approval_queue
2. f24_get_site_diary_approval_review
3. f25_get_site_diary_print_read
All improperly defined as SECURITY INVOKER in F2.4 and F2.5, resulting in `permission denied for schema private` when invoked by authenticated users via PostgREST.

Proof classification:
Live Sequential Stale-Token Proof.

Residual risks:
- SECURITY INVOKER defect in production migrations requires explicit architectural patching in B02.

Recommendation:
STOP FOR HQ REVIEW.

ABSOLUTE STOP.

DO NOT MERGE PR #32.
DO NOT START B02.
