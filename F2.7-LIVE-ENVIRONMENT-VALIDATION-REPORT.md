# F2.7-LIVE-ENVIRONMENT-VALIDATION-REPORT

## Mission Objective
F2.7-B01 aims to convert static/mocked authority proof into real PostgreSQL/Supabase execution proof, and establish reproducible synthetic UAT fixtures. 

## Environment Findings
- **Supabase CLI:** Available (version 2.109.1)
- **Docker / PostgreSQL:** Installed but daemon connection failed (
pipe:////./pipe/dockerDesktopLinuxEngine). 
- **Remote Environment:** The repository .env.local contains a reference to a remote Supabase instance (dihwyhlkoymedsxjuiul.supabase.co). Since this is not classified as a disposable local environment (Class A) or explicitly isolated staging environment (Class B), executing destructive mutations or untested migrations against it is strictly prohibited.

**Result:** LIVE EXECUTION BLOCKED BY ENVIRONMENT.

## Fixture/Security Preparation (UAT Bootstrap)
Although live database execution is blocked, the following UAT preparation artifacts were generated:

1. **supabase/uat_seed.sql**: A dedicated, idempotent, and deterministic UAT fixture script. It avoids modifying production schemas and does not override any existing seed.sql (which was confirmed absent). 
2. **Synthetic Personas**:
   - P1 — SUBMITTER: Mapped to SITE_SUPERVISOR role.
   - P2 — REVIEWER: Mapped to RESIDENT_ENGINEER role.
   - P3 — UNAUTHORIZED: Authenticated without Programme membership.
3. **Synthetic Dataset**: 
   - A canonical UAT Synthetic Programme A.
   - Current and Historical Revisions.
   - MSP and VO Activities.
   - Current and Historical Site Diaries.
   - Approval states mapped precisely to respect DB invariants (Pending, Returned, Rejected, Approved).
   - Workforce items correctly overflowing (10 Contractor, 7 NSC) for exact Print Continuation testing.

## Proof Classification (Current State)
- **STATIC PROOF:** Validated (via pnpm run verify contract tests).
- **MOCKED PROOF:** Validated (via existing integration suite).
- **LIVE DATABASE PROOF:** NOT EXECUTED / PENDING.
- **LIVE AUTH PROOF:** NOT EXECUTED / PENDING.
- **LIVE CONCURRENCY PROOF:** NOT EXECUTED / PENDING.

## Recommendations for Manual Bootstrap (B03)
Because Supabase Auth relies on uth.users with encrypted credentials, automated user creation without DB connection or privileged API tokens is not feasible in B01. The following procedure must be executed by HQ/Admin to initialize UAT:
1. Use Supabase Dashboard to create two test users: submitter@jkr.gov.my and eviewer@jkr.gov.my.
2. Retrieve their generated UUIDs and update _p1_user_id and _p2_user_id inside supabase/uat_seed.sql.
3. Execute supabase/uat_seed.sql via Supabase Dashboard SQL Editor or via psql.
