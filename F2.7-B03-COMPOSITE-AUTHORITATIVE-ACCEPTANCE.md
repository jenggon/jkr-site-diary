# F2.7-B03 — Composite Authoritative Acceptance

## Authority and scope

- Mission: `F2.7-B03-HC1 — P3 PRINT DEEP-LINK DENIAL CLOSURE + B03 ACCEPTANCE RECONCILIATION`
- Mode: bounded acceptance evidence only
- Canonical `develop` SHA: `0f9924468339a6ea028670b1f04e9285e21b4a24`
- Frozen V3 authoritative evidence SHA: `2631a2b64c85bb2910c96cd8b40e28df4d1e7d0a`
- Evidence branch: `uat/F2.7-B03H-p3-denial-closure`
- V3 evidence source read-only: `F2.7-B03-AUTHORITATIVE-ONE-SHOT-V3-UAT.md`

This is a composite authoritative acceptance checkpoint. B03-A through B03-G are inherited from the frozen V3 evidence and are not rerun by HC1. V3 also supplies the already-passing natural P3 Programme denial. HC1 is limited to closing the single harness-invalid B03-H canonical Print deep-link denial evidence gap. No full B03 replay is authorised.

## Preserved V3 state preconditions — read-only validation

The untouched local PostgreSQL state was validated with a read-only session before browser execution:

| Control | Exact preserved value | Result |
| --- | --- | --- |
| Site Diary ID | `9012210b-8375-42a2-a1fa-f4c1c9a289da` | Present exactly once |
| Programme ID | `a0000000-0000-4000-8000-000000000001` | Exact match |
| Revision ID | `b0000000-0000-4000-8000-000000000002` | Exact match |
| Activity ID | `d0000000-0000-4000-8000-000000000001` | Exact match |
| Activity date | `2026-08-28` | Exact match |
| Site Diary count for Activity/date | `1` | Exact match |
| Approval ID | `723bf152-28ab-4d21-bea9-586e9b32410f` | Present exactly once for the Site Diary |
| Approval count for Site Diary | `1` | Exact match |
| Approval status | `Approved` | Exact match |
| `requested_by` | `99999999-9999-9999-9999-999999999991` | Exact match |
| `approved_by` | `99999999-9999-9999-9999-999999999992` | Exact match |
| Legacy `weather` | `NULL` | Exact match |
| `print_context.weather_condition` | `ELOK` | Exact match |
| P3 Programme membership count | `0` | Exact match |

No state was reconstructed. No database reset, fixture load, insert, update, delete, truncate, Site Diary creation, or Approval creation was used.

## Canonical Print boundary — static lock

- Canonical page: `/site-diary/print?id=9012210b-8375-42a2-a1fa-f4c1c9a289da`
- Exact browser-generated API boundary: `GET /api/site-diary/9012210b-8375-42a2-a1fa-f4c1c9a289da/print`
- Canonical route: `src/app/api/site-diary/[siteDiaryId]/print/route.ts`
- The client constructs `/api/site-diary/${encodeURIComponent(id)}/print` and supplies the authenticated session access token.
- The route verifies the bearer identity and constructs the print repository with the authenticated caller access token.
- A foreign read may validly deny with HTTP `403` or `404`; HTTP `200` is prohibited.
- HTTP `403` UI mapping: `Akses ditolak. Anda tidak mempunyai kebenaran untuk melihat rekod ini.`
- HTTP `404` UI mapping: `Rekod tidak dijumpai.`
- Raw backend error text is not an accepted UI result.

## P3 closure persona

- Actor ID: `99999999-9999-9999-9999-999999999993`
- Email: `unauthorized@external.com`
- Programme A membership: none (`0` rows)
- Journey restriction: P3 only; no P4, P1, or P2 activity

## Frozen scratch runner

- Path: `C:\Users\user\.codex\visualizations\2026\08\29\01a04ca9-1f49-7090-9a66-394dbbccb75d\b03_hc1_p3_print_denial.cjs`
- SHA-256 before execution: `C93658EF8B950B44046E8052CBE35A96C1F9D6DE084390E328365ACE7D15E51E`
- Module preflight: `@playwright/test` available; Chromium executable present
- Syntax preflight: `node --check` passed
- Freeze rule: no runner edits after the recorded SHA-256
- Execution contract: exactly one runner invocation and one normal P3 browser journey, with no retry

## Closure acceptance contract

The frozen runner must:

1. Log in as P3 through the normal UI.
2. Observe the natural `GET /api/programme?status=Active` response at HTTP `200`, with Programme A absent from both response and visible UI.
3. Navigate to the exact canonical Print deep-link.
4. Observe the exact browser-generated Print API request ending in `/print`.
5. Require HTTP `403` or `404`, never `200`.
6. Require the status-matched bounded denial message.
7. Require that the target task, target notes, P1/P2 identities, weather/manpower content, raw SQLSTATE, PostgREST text, stack traces, and database internals are not exposed.
8. Preserve the exact V3 database state, confirmed by final read-only control.

## Acceptance reconciliation checkpoint

| Acceptance area | Evidence source | Status at checkpoint |
| --- | --- | --- |
| B03-A through B03-G | Frozen authoritative V3 evidence | `PASS` — inherited, not rerun |
| B03-H natural P3 Programme denial | Frozen authoritative V3 evidence | `PASS` — inherited |
| B03-H exact canonical Print deep-link denial | HC1 frozen runner | `UNDER EXECUTION` |
| Composite classification | V3 + HC1 | `PENDING HC1` |

This checkpoint does not claim that HC1 reran P4, P1, or P2. Only the B03-H Print deep-link evidence gap remains under execution.
