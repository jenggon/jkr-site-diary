# F4.5-B01B Adaptive Tactical Shell Report

## Verification and runtime basis

- Base SHA: `d2921125a43bb4212d003f37f4928dc9910dcad0`.
- Branch: `feature/F4.5-B01B-adaptive-tactical-shell`.
- Evidence runtime: localhost Next.js production server backed by localhost Supabase.
- Data basis: repository-owned synthetic fixtures from `supabase/seed.sql`, restored with `supabase db reset`.
- Authentication: actual GoTrue login through the application UI using the checked-in submitter and reviewer personas.
- Application data: actual Programme, Project Summary, Revision, Records, Approval, and New Entry API routes.
- No browser request interception, API mocking, or fake Auth/Supabase session is used by the B01B evidence harness.
- Product Owner visual acceptance remains pending.

## 1. Shell architecture

`DailyEntryShell.tsx` owns a `100dvh` adaptive shell. Its global header, Programme context, main workspace, and navigation remain in one bounded layout with independent content scrolling.

`SiteDiaryWorkspace.tsx` provides:

- a thumb-reachable bottom navigation at the mobile breakpoint, including safe-area bottom padding;
- a persistent desktop rail that is compact at `md` and expands at `lg`;
- a wider desktop operational workspace without changing internal workflow ownership.

The obsolete `BottomNavigation.tsx` remains removed.

## 2. Semantic shell layer

The intentionally small semantic Tailwind layer covers canvas/raised/interactive surfaces, tactical text, operational/selected accents, state feedback, and safe-area spacing. It is a shell vocabulary rather than a general-purpose design framework.

## 3. Revision context audit

The Programme context maps lifecycle presentation truthfully:

- `RESOLVED` plus a revision id: authoritative/current valid presentation;
- `RESOLVING`: neutral loading presentation;
- `UNAVAILABLE`: non-authoritative unavailable presentation;
- `ERROR`: error presentation;
- `IDLE`: neutral presentation.

Unresolved and unavailable states do not receive a green/valid authority treatment. No revision lifecycle or domain semantics were changed.

## 4. Approval navigation authority

`AuthContext` still exposes the Supabase session/user rather than a canonical frontend role or capability. B01B therefore does not invent `user.role`, `user.canApprove`, email allowlists, or guessed JWT claims.

`Kelulusan` remains visible. UI visibility is not authorization; backend capability and membership enforcement remain the security authority. Role-sensitive navigation is recorded as UX debt outside this shell package.

## 5. Deterministic real-runtime evidence harness

`scripts/capture-screenshots.ts` now:

- launches an unmodified browser against `http://localhost:3000` (or `B01B_BASE_URL`);
- logs in through the actual UI with seeded synthetic personas;
- selects the real Programme and its current or historical revision;
- requires real localhost GoTrue and application API responses;
- performs semantic assertions before every screenshot;
- fails the process on any required navigation, state, API, or visible-error failure;
- verifies the mobile Records and New Entry images have different SHA-256 hashes.

The previous request interception, fake session, fail-soft interaction handling, redundant selected-nav capture, and mislabeled history capture are removed.

## 6. Runtime visual review

### Mobile 375 × 812

- The global header is compact and Programme/revision context remains readable without consuming most of the viewport.
- The four bottom targets are reachable and maintain 56 px minimum target height; labels fit at the required width.
- Selected state is clear through icon/text treatment while preserving a restrained field-tool character.
- Safe-area/content padding prevents the fixed navigation from hiding the final content.
- Browser geometry assertions show no horizontal overflow.
- The density is operational rather than decorative and does not become game-like. New Entry remains intrinsically dense because its existing source/task workflow is outside the B01B shell scope.

### Desktop 1280 × 720

- The expanded `lg:w-64` rail makes destination labels immediately scannable; the `md:w-20` compact state remains appropriate for narrower desktop/tablet widths.
- The `max-w-5xl` content frame uses the available width at 1280 px without creating a giant-phone column.
- Programme/revision hierarchy and selected navigation are unambiguous.
- Content scrolling is contained inside the workspace while the header/context/rail remain stable.
- Records and Approval framing is coherent. The single seeded approval naturally leaves empty vertical space, but the queue item and review control are prominent and usable.

## 7. Runtime evidence

Fresh genuine screenshots under `docs/evidence/f4.5-b01b/`:

- `mobile-shell-records-375x812.png` — submitter, current revision, Records selected, current records rendered, mobile navigation visible.
- `mobile-shell-new-entry-375x812.png` — submitter, New Entry selected, MSP/VO source workflow rendered, mobile navigation visible.
- `desktop-shell-records-1280x720.png` — submitter, desktop rail, current revision, current records.
- `desktop-shell-approval-1280x720.png` — reviewer, desktop rail, pending queue and review control.
- `desktop-shell-history-1280x720.png` — submitter, Semakan Terdahulu selected, actual superseded revision selected, historical records rendered.

`mobile-shell-selected-nav-375x812.png` was removed because it was redundant rather than an additional proof state.

## 8. Scope confirmation

No database, migration, RLS/RBAC, Programme lifecycle, Activity/Site Diary mutation, MSP/VO authority, Task Picker, Workforce, Weather, NSC, approval semantics, Records repository semantics, PDF, official Page 1, or continuation-output rule was changed.

## 9. Final gates

- `pnpm run verify`: PASS (140 test files, 1,141 tests, production build complete).
- `git diff --check`: PASS.
- HQ and Product Owner visual review: pending; no claim of aesthetic acceptance is made.
